# Local Development Environment Variables
# Copy this file to .dev.vars and fill in values
# DO NOT commit .dev.vars to git

# ===========================================
# Auth.js Configuration
# ===========================================

# Session encryption secret (REQUIRED)
# Must be at least 32 characters, cryptographically random
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-secret-here

# Auth.js URL (set automatically in Cloudflare, needed for local dev)
AUTH_URL=http://localhost:8787

# ===========================================
# Google OAuth
# ===========================================
# Create at: https://console.cloud.google.com/apis/credentials
# Add authorized redirect URIs:
#   - http://localhost:3000/api/auth/callback/google
#   - https://your-domain.com/api/auth/callback/google

GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ===========================================
# Microsoft Entra ID (Azure AD) OAuth
# ===========================================
# Create at: https://portal.azure.com/#blade/Microsoft_AAD_RegisteredApps

AZURE_AD_CLIENT_ID=your-azure-client-id
AZURE_AD_CLIENT_SECRET=your-azure-client-secret
AZURE_AD_TENANT_ID=your-azure-tenant-id

# ===========================================
# Feature Flags
# ===========================================
# Accepts: "1", "true", "on" (case-insensitive) as truthy
# All other values (including "false", "0", "off") are falsy
# Default: OFF (when not set)

# Master kill switch - when OFF, all Today features are disabled
FLAG_TODAY_FEATURES_MASTER=true

# Individual feature flags (only active when master is ON)
FLAG_TODAY_DECISION_SUPPRESSION_V1=true
FLAG_TODAY_NEXT_ACTION_RESOLVER_V1=true
FLAG_TODAY_MOMENTUM_FEEDBACK_V1=true
FLAG_TODAY_SOFT_LANDING_V1=true
FLAG_TODAY_REDUCED_MODE_V1=true

